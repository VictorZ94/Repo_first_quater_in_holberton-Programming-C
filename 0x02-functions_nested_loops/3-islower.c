#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */

int _islower(int r)
{
	if ( _islower(r) )
	{
	_putchar(49);
	}
	else
	{
	_putchar(48);
	}
return(0);
}
