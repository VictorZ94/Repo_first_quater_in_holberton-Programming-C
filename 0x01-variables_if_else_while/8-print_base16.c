#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int hex;

	for (hex = 48; hex <= 57; hex++)
	{
	putchar(hex);
	}	
		for (hex = 65; hex <= 70; hex++)
		{
		putchar(hex);
		}
		putchar('\n');
	return (0);
}
