#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int alpr;

	for (alpr = 'z'; alpr >= 'a'; alpr--)
	{
	putchar(alpr);
	}
	putchar('\n');
return (0);
}
